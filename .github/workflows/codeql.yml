name: "Call CodeQL Advanced Shared Workflow"

on:
  push:
    branches:
      - 'main'
  schedule:
    - cron: "0 0 * * *"
  pull_request_target:
    types: [ opened, synchronize, reopened ]
  workflow_dispatch:


jobs:
  trigger-codeql:
    uses: zendesk/prodsec-code-scanning/.github/workflows/codeql_advanced_shared.yml@main